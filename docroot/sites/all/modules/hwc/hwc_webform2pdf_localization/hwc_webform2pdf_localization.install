<?php

/**
 * Increase module weight to execute after all other hooks
 */
function hwc_webform2pdf_localization_update_7001() {
  db_update('system')
    ->fields(array('weight' => 10))
    ->condition('name', 'hwc_webform2pdf_localization', '=')
    ->execute();
}
